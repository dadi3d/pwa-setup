version: '3.8'

services:
  nginx:
    build: .
    container_name: pwa-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /home/administrator/ssl:/etc/nginx/ssl:ro
    networks:
      - ausleihe
    restart: unless-stopped

networks:
  ausleihe:
    external: true